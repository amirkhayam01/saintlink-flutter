targets:
  $default:
    builders:
      json_serializable:
        options:
          # The API is snake_case throughout, with one exception the model
          # itself annotates (VehicleCategory).
          field_rename: snake
          explicit_to_json: true
