// Copy to Secrets.xcconfig (ignored by git) and fill in.
// The Maps SDK for iOS key, restricted in the Google console to this app's
// bundle id and to that one API. It reaches the SDK via Info.plist.
GOOGLE_MAPS_IOS_KEY=
